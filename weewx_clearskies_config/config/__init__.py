"""Config CRUD subpackage — read and update existing .conf files.

Provides:
  reader   — read and parse component .conf files
  updater  — MANAGED REGION merge logic
  routes   — FastAPI router at /admin/config
"""
